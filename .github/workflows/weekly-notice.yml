name: Weekly CAU Notices

on:
  schedule:
    # Run every Friday at 8:00 AM KST (Thursday 23:00 UTC)
    # KST = UTC+9, so 8:00 KST = 23:00 UTC
    - cron: "0 23 * * 4"
  workflow_dispatch:

jobs:
  run-weekly-notice:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js v20
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Run CAU Notice Bot
        env:
          SMTP_USER: ${{ secrets.EMAIL_USER }}
          SMTP_PASS: ${{ secrets.EMAIL_PASS }}
          MAIL_TO: ${{ secrets.EMAIL_TO }}
        run: npm run start

